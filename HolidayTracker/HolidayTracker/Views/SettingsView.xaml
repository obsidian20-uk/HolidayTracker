<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="HolidayTracker.Views.SettingsView"
             xmlns:base="clr-namespace:HolidayTracker.ViewModels;assembly=HolidayTracker"
             base:ViewModelLocator.AutoWireViewModel="true"
             Title="{Binding Title}">
    <StackLayout>
        <Label Text="Work Weekends?"/>
        <Switch IsToggled="{Binding WorkWeekends, Mode=TwoWay}"/>
        <Label Text="Work Public Holidays?"/>
        <Switch IsToggled="{Binding WorkPublicHolidays, Mode=TwoWay}"/>
        <Label Text="SETTINGS WILL ONLY APPLY TO HOLIDAYS CREATED OR EDITED FROM THIS POINT"/>

        <Button Text="Update Settings" Command="{Binding cmdUpdateSettings}"/>
        <Picker Title="Country" ItemsSource="{Binding Countries}" ItemDisplayBinding="{Binding Value}" SelectedItem="{Binding Country}"/>
        <Button Text="Update Public Holidays" Command="{Binding cmdUpdatePublicHolidays}"/>

    </StackLayout>
</ContentPage>

<!--x:Name="MyCreateHolidayView"-->